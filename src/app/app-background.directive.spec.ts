import { AppBackgroundDirective } from './app-background.directive';

describe('AppBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new AppBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
